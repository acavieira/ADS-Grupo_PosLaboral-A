/*import RepositoryOverviewCard from './RepositoryOverviewCard.vue';

export default {
  title: 'Components/RepositoryOverviewCard',
  component: RepositoryOverviewCard,
};

const Template = (args: any) => ({
  components: { RepositoryOverviewCard },
  setup() {
    return { args };
  },
  template: `<RepositoryOverviewCard v-bind="args" />`,
});

export const Default = Template.bind({});
Default.args = {
  repo: {
    name: 'awesome-project',
    description: 'A React project with TypeScript',
    stars: 1234,
    forks: 234,
    issues: 18,
    prs: 12,
    commits: 45,
    language: 'TypeScript',
  },
};*/
