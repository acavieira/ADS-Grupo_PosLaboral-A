/*import BaseLayout from './BaseLayout.vue';
import BaseCard from '../BaseCard/BaseCard.vue';

export default {
  title: 'Components/BaseLayout',
  component: BaseLayout,
  argTypes: {},
};

const Template = (args: any) => ({
  components: { BaseLayout, BaseCard },
  setup() {
    return { args };
  },
  template: `
    <BaseLayout>
      <BaseCard class="pa-4 text-center">
        <h3>Base Layout Example</h3>
        <p>This layout wraps content in responsive container</p>
      </BaseCard>
    </BaseLayout>
  `,
});

export const Default = Template.bind({});
Default.args = {};*/
